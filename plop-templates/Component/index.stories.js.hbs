import React from 'react'

import {{pascalCase name}} from '.'

export default {
title: 'UI Library/{{pascalCase name}}',
component: {{pascalCase name}}
}

const Template = args =>
<{{pascalCase name}} {...args} />

export const Default = Template.bind({})
Default.args = {
examplePropText: 'example text'
}